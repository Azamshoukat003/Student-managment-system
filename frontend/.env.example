# In dev, requests to /api are proxied to the backend (see vite.config.js),
# so this can stay as /api. For a deployed frontend, set it to the full backend
# URL, e.g. https://your-api.onrender.com/api
VITE_API_BASE_URL=/api
